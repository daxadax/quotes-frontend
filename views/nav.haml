%ul.nav.navbar-nav
  %li= link_to "/users/#{current_user.nickname}", current_user.nickname if current_user
  %li= link_to '/login', 'Login' unless current_user
  %li= link_to '/tags',  'Tags'
  %li.dropdown
    %a.dropdown-toggle{:role => 'button',
                                    :aria => {:expanded => 'false'},
                                    :data => {:toggle => 'dropdown'}}
      Publications
      %span.caret
      %ul.dropdown-menu{:role => 'menu'}
        %li= link_to '/publications', 'View All'
        %li= link_to '/new/publication', 'Add a new publication'

  %li.dropdown
    %a.dropdown-toggle{:role => 'button',
                                    :aria => {:expanded => 'false'},
                                    :data => {:toggle => 'dropdown'}}
      Quotes
      %span.caret
      %ul.dropdown-menu{:role => 'menu'}
        %li= link_to '/quotes', 'View All'
        %li= link_to '#', "Added by me" if current_user
        %li= link_to '/new/quote', 'Add a new quote'
