.row
  .quote-title.col-xs-10= show_publication_information_for quote
  .btn-group.col-xs-2
    %button.btn.btn-primary.dropdown-toggle{:type => 'button', :data =>{:toggle => 'dropdown'}}
      Options
      %span.caret
    %ul.dropdown-menu
      %li=link_to '#', 'edit'
      %li=link_to '#', 'delete'
      %li=link_to "/quote/#{quote.uid}", 'permalink'

%blockquote.clear
  %p= markdown quote.content
%ul.nav.navbar-nav
  - quote.tags.each do |tag|
    %li= link_to "/tag/#{tag}", tag
